<!DOCTYPE html>
<html>
  <head>
    <title>TestFirebase</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://use.fontawesome.com/releases/v5.6.1/css/all.css" rel="stylesheet">
  </head>

  <body>
    <h3 style="background-color: #CBFFD3;"><%= notice %></h3>
    <ul>
      <li>
         <%= link_to("ホーム", "/") %>
      </li>
      <% if current_user %>
        <li>
          <%= link_to(current_user.name, "/users/#{current_user.id}/show") %>
        </li>
        <li>
          <%= link_to "Logout", :logout, method: :post %>
        </li>
    <% else %>
      <li>
        <%= link_to "新規登録", users_new_path %>
      </li>
      <li>
        <%= link_to "ログイン", :login %>
      </li>
      <li>
        <%= link_to auth_at_provider_path(provider: :google), class: 'google-btn', style: "color: #AAA;" do %>
          <h3>
            <img src="https://img.icons8.com/color/25/000000/google-logo.png" style="vertical-align: top;"/> Googleでログイン
          </h3>
        <% end %>
        <a href="https://icons8.com/icon/17949/googleのロゴ" target="_blank">Googleのロゴ icon by Icons8</a>
      </li>
    </ul>
    <% end %>
    <%= yield %>
  </body>
</html>
